export class FormateJson {
  public json = []

  /**
   * @description 初始化传入的 json url 数据
   *
   * @param {string} [url='']
   * @memberof FormateJson
   */
  init (url:string = '') {
  }
}